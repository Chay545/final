# Curavida Home Health (local copy)

Build

```
npm install
npm run build
```

The `build` script compiles Tailwind from `style.css` into `output.css`.

Deploy

- Configure your deployment (Vercel/GitHub) to run `npm run build` before serving.
